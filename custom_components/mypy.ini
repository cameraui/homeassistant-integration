[mypy]
; Ensure full coverage
disallow_untyped_calls = True
disallow_untyped_defs = True
disallow_incomplete_defs = True
disallow_untyped_decorators = True
check_untyped_defs = True

; Restrict dynamic typing
disallow_any_generics = True
disallow_subclassing_any = False
warn_return_any = True

; Know exactly what you're doing
warn_redundant_casts = True
warn_unused_ignores = False
warn_unused_configs = True
warn_unreachable = True
show_error_codes = True

; Explicit is better than implicit
no_implicit_optional = True

# Allows Type[T] to refer to abstract classes, which is not otherwise supported.
# See https://github.com/python/mypy/issues/4717
disable_error_code = import-untyped,unused-ignore,no-any-return,redundant-cast,typeddict-item,type-abstract
exclude = test
mypy_path = $MYPY_CONFIG_FILE_DIR
explicit_package_bases = True
namespace_packages = True

python_version = 3.14
; python-socketio ships no type information, so its decorators are untyped.
; Scoped to the one module that talks to it instead of relaxing this globally.
[mypy-cameraui.api]
disable_error_code = untyped-decorator
